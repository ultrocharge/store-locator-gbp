import { DefaultSettingsGlobalActionContext } from "gadget-server";

/**
 * @param { DefaultSettingsGlobalActionContext } context
 */
export async function run({ params, logger, api, connections }) {
  // Your logic goes here
};
